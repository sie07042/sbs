# ==========================================
# 프로덕션 환경 변수 (Docker 컨테이너용)
# ==========================================
# Vite는 VITE_ 접두사가 붙은 환경 변수만 클라이언트에 노출합니다.
# 빌드 타임에 이 값들이 번들에 포함됩니다.

# 애플리케이션 모드
NODE_ENV=production

# API 베이스 URL
# Docker 컨테이너에서는 /api로 시작하는 요청이
# nginx.conf의 proxy_pass 설정에 의해 백엔드로 전달됩니다.
# Vite proxy 설정은 개발 환경에서만 동작하므로,
# 프로덕션에서는 Nginx가 프록시 역할을 합니다.
VITE_API_BASE_URL=/api

# 애플리케이션 제목 (선택 사항)
VITE_APP_TITLE=SBS Application

# 카카오 관련 설정 (클라이언트 사이드)
# 주의: 민감한 정보는 절대 노출하지 마세요!
# Client ID는 공개되어도 괜찮지만, Secret Key는 백엔드에만 보관해야 합니다.
# VITE_KAKAO_CLIENT_ID=your_kakao_client_id
